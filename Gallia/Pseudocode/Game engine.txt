public class engine{

	main method{
		print "Hello adventurer, would you like to start a new journey or load one in?"
		call input method from character class

		do{
			if input contains "start" or "new"{
				invalid selection variable = false
				call start method from character class
			}
			else if input contains "load"{
				invalid selection variable = false
				call load method from character class
			}
			else{
				print "invalid selection"
				invalid selection variable = true
			}
		}while selection is invalid
	}
}